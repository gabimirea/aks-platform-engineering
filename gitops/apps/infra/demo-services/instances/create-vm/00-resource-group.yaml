apiVersion: azure.upbound.io/v1beta1
kind: ResourceGroup
metadata:
  name: rg-vm-sdc-001
  namespace: default
  labels:
    platform.demo/request: create-vm
spec:
  forProvider:
    name: rg-vm-sdc-001
    location: swedencentral
    tags:
      COST: T3
  providerConfigRef:
    name: default
