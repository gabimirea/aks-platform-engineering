apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: aks1-appset
  namespace: argocd
spec:
  generators:
    - clusters:
        selector:
          matchLabels:
            environment: control-plane
  template:
    metadata:
      name: aks1
      namespace: argocd
    spec:
      project: default
      source:
        repoURL: '{{metadata.annotations.addons_repo_url}}'
        targetRevision: '{{metadata.annotations.addons_repo_revision}}'
        path: gitops/clusters/crossplane/aks1/claim
      destination:
        name: '{{name}}'
        namespace: workload
      syncPolicy:
        retry:
          limit: 10
        automated: {}
        syncOptions:
          - CreateNamespace=true
