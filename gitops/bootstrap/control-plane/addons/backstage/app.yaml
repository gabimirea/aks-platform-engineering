apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: backstage
  namespace: argocd
  labels:
    app.kubernetes.io/name: backstage
    app.kubernetes.io/part-of: platform-addons
    app.kubernetes.io/managed-by: argocd
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: backstage
  source:
    repoURL: https://github.com/backstage/charts
    targetRevision: a450c6408e3ef7b10c45d7fd4bf87568ebb24d08
    path: charts/backstage
    helm:
      values: |
        backstage:
          image:
            registry: ecdcrepository.azurecr.io
            repository: dev-portal/backstage
            tag: "1.0.14"
            pullPolicy: Always
          extraEnvVars:
            - name: APP_BASE_URL
              value: http://backstage.local:7007
            - name: NODE_OPTIONS
              value: --no-node-snapshot
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: backstage-github
                  key: token

          appConfig:
            app:
              title: "Developer Portal"
              baseUrl: ${APP_BASE_URL:-http://localhost:7007}
            integrations:
              github:
                - host: github.com
                  token: ${GITHUB_TOKEN}
            auth:
              providers:
                guest:
                  dangerouslyAllowOutsideDevelopment: true
            backend:
              baseUrl: ${APP_BASE_URL:-http://localhost:7007}
              csp:
                upgrade-insecure-requests: false
              reading:
                allow:
                  - host: raw.githubusercontent.com
                  - host: github.com
              cors:
                origin: ${APP_BASE_URL:-http://localhost:7007}
                methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
                credentials: true
            catalog:
              locations:
                - type: url
                  target: https://github.com/backstage/software-templates/blob/main/scaffolder-templates/react-ssr-template/template.yaml
                  rules:
                    - allow: [Template]
                - type: url
                  target: https://github.com/gabimirea/aks-platform-engineering/blob/main/backstage/templates/azure-demo-services/template.yaml
                  rules:
                    - allow: [Template]
                - type: url
                  target: https://github.com/gabimirea/aks-platform-engineering/blob/main/gitops/apps/myapp/postgres/catalog-info.yaml
                  rules:
                    - allow: [Component, System, Resource, API, Location, Group]
            techdocs:
              builder: local
              generator:
                runIn: local
              publisher:
                type: local

        service:
          type: LoadBalancer

        ingress:
          enabled: false
          host: backstage.local
          path: /
          tls:
            enabled: false

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
